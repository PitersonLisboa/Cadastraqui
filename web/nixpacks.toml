# Configuração do Nixpacks para Railway
# Garante que o build use as variáveis VITE_* e o start use serve com SPA fallback

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -s -l ${PORT:-8080}"
