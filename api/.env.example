# ===========================================
# CADASTRAQUI v2 - Variáveis de Ambiente
# ===========================================

# Ambiente (development | production)
NODE_ENV=development

# Servidor
PORT=3000
HOST=0.0.0.0

# ===========================================
# BANCO DE DADOS (Supabase)
# ===========================================
# Copie a Connection String do Supabase
# Dashboard → Settings → Database → Connection string → URI
# IMPORTANTE: Use porta 6543 (pooling), NÃO 5432
DATABASE_URL="postgresql://postgres.[SEU-PROJETO]:[SUA-SENHA]@aws-0-sa-east-1.pooler.supabase.com:6543/postgres"

# ===========================================
# AUTENTICAÇÃO JWT
# ===========================================
# Gerar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua-chave-secreta-muito-segura-aqui-minimo-64-caracteres
JWT_EXPIRES_IN=7d

# ===========================================
# FRONTEND URL (CORS)
# ===========================================
# Desenvolvimento: http://localhost:5173
# Produção: https://app.cadastraqui.net.br
FRONTEND_URL=http://localhost:5173

# ===========================================
# EMAIL SMTP (Opcional)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app
MAIL_FROM="Cadastraqui <noreply@cadastraqui.net.br>"

# ===========================================
# SUPABASE STORAGE (para arquivos)
# ===========================================
# Dashboard → Settings → API → Project URL e service_role key
SUPABASE_URL=https://[SEU-PROJETO].supabase.co
SUPABASE_SERVICE_KEY=sua-service-role-key
